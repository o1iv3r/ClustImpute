# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages

r:
  - oldrel
  - release
  - devel

r_github_packages:
  - r-lib/covr

r_packages:
  - libgsl0-dev

r_binary_packages:
  - copula

after_success:
  - Rscript -e 'covr::codecov()'
